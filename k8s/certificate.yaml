apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: github-stats

spec:
  secretName: github-stats-tls

  dnsNames:
    - github-stats.167.233.211.87.nip.io

  issuerRef:
    name: letsencrypt-production
    kind: Issuer
